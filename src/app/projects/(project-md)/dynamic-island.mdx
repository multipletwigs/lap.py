import {
  AnimatedIcons,
  AnimatedLayout,
} from "@/app/projects/(components)/dynamic-island";

# Dynamic Island

An attempt to recreate the Dynamic Island Component.

<br />

But before we go into the dynamic island, we should talk a little about Framer Motion. Here's an example of `~initial` and `~end` animations that we commonly see for icon transitions.

<AnimatedIcons />

<br />

## My favourite songs this month :)

Despite the optical illusion of elements transitioning, they are actually two separate components! Extrapolated through the same `~layout id`!

<AnimatedLayout />
